{
  "name": "healthcare-crm",
  "version": "1.0.0",
  "description": "Healthcare CRM - Patients Notes Management",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "start": "npm run dev:frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run offline --prefix backend",
    "test": "npm test --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix infrastructure",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "deploy:infra": "npm run build --prefix backend && cd infrastructure && npx cdk deploy --require-approval never",
    "bootstrap:cdk": "cd infrastructure && npx cdk bootstrap"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
